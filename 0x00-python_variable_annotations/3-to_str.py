#!/usr/bin/env python3
"""a function that takes a float n as argument and returns the string rep"""


def to_str(n: float) -> str:
    """returns a string representation of a float
    Args:
        n: float to convert to string
    Returns:
        a string representation of a float
    """
    return str(n)
